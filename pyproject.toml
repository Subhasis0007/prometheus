[project]
name = "prometheus-workspace"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = [
    "flask>=3.1.3",
    "langgraph>=1.1.10",
    "rich>=15.0.0",
    "typer>=0.25.1",
]

[tool.uv.workspace]
members = [
    "packages/core",
    "packages/agents/*",
    "apps/daemon"
]
